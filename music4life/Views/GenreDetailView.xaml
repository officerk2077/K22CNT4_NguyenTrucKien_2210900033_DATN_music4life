<UserControl x:Class="music4life.Views.GenreDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <SolidColorBrush x:Key="Brush.Text.Secondary" Color="#B3B3B3"/>
        <SolidColorBrush x:Key="Brush.Accent" Color="#1DB954"/>
        <SolidColorBrush x:Key="Brush.Bg.Tertiary" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="Brush.Hover" Color="#2A2A2A"/>

        <Style x:Key="ColumnSplitterStyle" TargetType="GridSplitter">
            <Setter Property="Width" Value="1"/>
            <Setter Property="Background" Value="#444"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>

        <Style x:Key="BigPlayButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Background" Value="{StaticResource Brush.Accent}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="28">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.4"/>
                                </Border.Effect>
                            </Border>
                            <Path Data="M8,5.14V19.14L19,12.14L8,5.14Z" 
                                  Fill="White" Width="20" Height="20" Stretch="Uniform" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1ED760"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05" CenterX="28" CenterY="28"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#169C46"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="28" CenterY="28"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GenreSongItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="0">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.Hover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212" Grid.IsSharedSizeScope="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="280"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="HeaderGrid">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF4B1F" Offset="0.0"/>
                    <GradientStop Color="#121212" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Button HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30,30,0,0"
                    Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnBack_Click">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M20,11V13H8L13.5,18.5L12.08,19.92L4.16,12L12.08,4.08L13.5,5.5L8,11H20Z" 
                          Fill="White" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Back to Genres" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter Opacity="{TemplateBinding Opacity}"/>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel VerticalAlignment="Bottom" Margin="30,0,0,30">
                <TextBlock Text="GENRE" FontSize="12" FontWeight="Bold" Foreground="White" Opacity="0.8"/>
                <TextBlock x:Name="txtGenreTitle" Text="Genre Name" FontSize="60" FontWeight="Black" Foreground="White" Margin="0,-5,0,10">
                    <TextBlock.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="The biggest hits from the world's biggest stars." Foreground="#DDD" FontSize="14"/>
            </StackPanel>

            <Button Style="{StaticResource BigPlayButtonStyle}" 
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,40,30"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10" HorizontalAlignment="Center" Width="950">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="300" SharedSizeGroup="GenreArtistColumn"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="#" Foreground="{DynamicResource Brush.Text.Secondary}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="TITLE" Foreground="{DynamicResource Brush.Text.Secondary}" FontWeight="SemiBold" Margin="10,0,0,0"/>

            <GridSplitter Grid.Column="2" Style="{StaticResource ColumnSplitterStyle}"/>

            <TextBlock Grid.Column="3" Text="ARTIST" Foreground="{DynamicResource Brush.Text.Secondary}" FontWeight="SemiBold" Margin="10,0"/>

            <TextBlock Grid.Column="4" Text="🕓" Foreground="{DynamicResource Brush.Text.Secondary}" FontWeight="SemiBold" HorizontalAlignment="Center"/>

            <Rectangle Grid.ColumnSpan="5" Height="1" Fill="{DynamicResource Brush.Bg.Tertiary}" VerticalAlignment="Bottom" Margin="0,10,0,-5"/>
        </Grid>

        <ListView x:Name="LvGenreSongs" Grid.Row="2" 
                  Margin="0,0,0,20"
                  Width="950" HorizontalAlignment="Center"
                  Background="Transparent" 
                  BorderThickness="0" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ItemContainerStyle="{StaticResource GenreSongItemStyle}">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition SharedSizeGroup="GenreArtistColumn"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding Number}" Foreground="{StaticResource Brush.Text.Secondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="White" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="10,0,20,0" VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="3" Text="{Binding Artist}" Foreground="{StaticResource Brush.Text.Secondary}" TextTrimming="CharacterEllipsis" Margin="10,0" VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="4" Text="{Binding Duration}" Foreground="{StaticResource Brush.Text.Secondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>